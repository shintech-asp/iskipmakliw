@{
    ViewData["Title"] = "3D Customization";
    Layout = "_LayoutCustomer";
}
@model List<ProductModel>
<div class="container py-5">
    <div class="row g-4">
        <!-- 3D Viewer -->
        <div class="col-xl-9 col-lg-8 col-md-12">
            <div class="card shadow-sm border-0 rounded-4">
                <div class="card-body p-4">
                    <h4 class="mb-3 fw-semibold">🪑 Customize Your Own Furniture</h4>
                    <canvas id="viewer" class="w-100 rounded-3" style="height: 600px;"></canvas>
                </div>
            </div>
        </div>

        <!-- Customization Controls -->
        <div class="col-xl-3 col-lg-4 col-md-12">
            <div class="card shadow-sm border-0 rounded-4">
                <div class="card-body p-4">
                    <!-- Model Selector -->
                    <h5 class="fw-semibold mb-3">Select Model</h5>
                    <div class="dropdown w-100 mb-3">
                        <button class="btn btn-outline-primary dropdown-toggle w-100" type="button" id="modelDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                            Select Furniture
                        </button>
                        <ul class="dropdown-menu w-100" aria-labelledby="modelDropdown">
                            @if(Model.Count >0)
                            {
                                @foreach(var data in Model)
                                {
                                    <li><a class="dropdown-item" href="#" onclick="loadModel('@data.ImagePath')">@data.ModelName</a></li>
                                }
                            }
                            else
                            {
                                <li class="fst-italic">Sorry, there's no model available for crafting from this seller.</li>
                            }
                        </ul>
                    </div>

                    <!-- Color -->
                    <h5 class="fw-semibold mt-4 mb-2">Change Color</h5>
                    <input type="color" id="colorPicker" class="form-control form-control-color mb-3" value="#ffffff" onchange="changeColor(this.value)">

                    <!-- Texture -->
                    <h5 class="fw-semibold mt-4 mb-2">Apply Texture</h5>
                    <div class="d-flex flex-wrap gap-2 mb-3">
                        <img src="/textures/oak.jpg" class="img-thumbnail texture-thumb" onclick="applyTexture('oak')" />
                        <img src="/textures/wood_table_worn.jpg" class="img-thumbnail texture-thumb" onclick="applyTexture('wood_table_worn')" />
                        <img src="/textures/spanish_cedar.jpg" class="img-thumbnail texture-thumb" onclick="applyTexture('spanish_cedar')" />
                        <img src="/textures/walnut.jpg" class="img-thumbnail texture-thumb" onclick="applyTexture('walnut')" />
                        <img src="/textures/white_ash.jpg" class="img-thumbnail texture-thumb" onclick="applyTexture('white_ash')" />
                    </div>

                    <!-- Resizing -->
                    <h5 class="fw-semibold mt-4 mb-2">Resize</h5>
                    <div class="mb-2">
                        <label class="form-label">Overall Size</label>
                        <input type="range" id="scaleSlider" class="form-range" min="0.5" max="2" step="0.1" value="1" onchange="changeScale(this.value)">
                        <small>+</small><small id="scaleValue" class="text-muted">1.0</small><small>meter/s</small>
                    </div>
                    <div class="mb-2">
                        <label class="form-label">Width (X)</label>
                        <input type="range" id="widthSlider" class="form-range" min="0.5" max="2" step="0.1" value="1" onchange="changeWidth(this.value)">
                        <small>+</small><small id="widthValue" class="text-muted">1.0</small><small>meter/s</small>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Height (Y)</label>
                        <input type="range" id="heightSlider" class="form-range" min="0.5" max="2" step="0.1" value="1" onchange="changeHeight(this.value)">
                        <small>+</small><small id="heightValue" class="text-muted">1.0</small><small>meter/s</small>
                    </div>

                    <!-- Buttons -->
                    <div class="d-flex justify-content-between">
                        <button class="btn btn-outline-secondary w-50 me-2" onclick="resetDimensions()">Reset</button>
                        <button class="btn btn-success w-50" type="button" onclick="submitCustomization()">Submit</button>
                    </div>

                    <!-- Hidden Form -->
                    <form id="customizationForm" method="post" asp-action="Customization" asp-controller="Home">
                        <input type="hidden" name="Model" id="selectedModel" />
                        <input type="hidden" name="color" id="selectedColor" />
                        <input type="hidden" name="texture" id="selectedTexture" />
                        <input type="hidden" name="Dimension" id="selectedDimension" />
                        <input type="hidden" name="Id" value="@ViewBag.Id" />
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Styling -->
<style>
    .texture-thumb {
        width: 60px;
        height: 60px;
        cursor: pointer;
        object-fit: cover;
        transition: transform 0.2s ease;
    }

        .texture-thumb:hover {
            transform: scale(1.05);
            border: 2px solid #0d6efd;
        }
</style>

@section Scripts {
    <!-- jQuery (required for submitCustomization) -->
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>

    <!-- Import Map for Three.js -->
    <script type="importmap">
        {
            "imports": {
                "three": "https://cdn.jsdelivr.net/npm/three@0.150.1/build/three.module.js"
            }
        }
    </script>

    <script type="module">
        import * as THREE from "three";
        import { GLTFLoader } from "https://cdn.jsdelivr.net/npm/three@0.150.1/examples/jsm/loaders/GLTFLoader.js";
        import { OrbitControls } from "https://cdn.jsdelivr.net/npm/three@0.150.1/examples/jsm/controls/OrbitControls.js";

        // ---------------------
        // 🧩 Global Variables
        // ---------------------
        let scene, camera, renderer, controls, model;
        let selectedModelPath = "";
        let selectedColor = "#ffffff";
        let selectedTexture = "";
        let originalModelSize = null;
        let texturePath = "";
        const dimensions = { scale: 1.0, width: 1.0, height: 1.0 };
        const canvas = document.getElementById("viewer");

        // Store original material colors for each mesh
        let originalMaterialColors = new Map();

        // ---------------------
        // 🎬 Scene Setup
        // ---------------------
        scene = new THREE.Scene();
        scene.background = new THREE.Color(0xf0f2f5);

        camera = new THREE.PerspectiveCamera(50, canvas.clientWidth / canvas.clientHeight, 0.1, 1000);
        camera.position.set(0, 1.5, 4);

        renderer = new THREE.WebGLRenderer({ canvas, antialias: true, alpha: true });
        renderer.setSize(canvas.clientWidth, canvas.clientHeight);
        renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
        renderer.outputEncoding = THREE.sRGBEncoding;
        renderer.toneMapping = THREE.ACESFilmicToneMapping;
        renderer.toneMappingExposure = 1.2;
        renderer.shadowMap.enabled = true;

        // Lighting setup
        const ambient = new THREE.AmbientLight(0xffffff, 0.8);
        const hemiLight = new THREE.HemisphereLight(0xffffff, 0x444444, 0.6);
        hemiLight.position.set(0, 20, 0);

        const dirLight = new THREE.DirectionalLight(0xffffff, 1.5);
        dirLight.position.set(5, 10, 7.5);
        dirLight.castShadow = true;
        dirLight.shadow.mapSize.width = 2048;
        dirLight.shadow.mapSize.height = 2048;

        const rimLight = new THREE.DirectionalLight(0x667eea, 0.5);
        rimLight.position.set(-5, 5, -5);

        scene.add(ambient, hemiLight, dirLight, rimLight);

        // Controls
        controls = new OrbitControls(camera, renderer.domElement);
        controls.enableDamping = true;
        controls.dampingFactor = 0.05;
        controls.autoRotate = true;
        controls.autoRotateSpeed = 1.0;

        // ---------------------
        // 🧱 Model Loading
        // ---------------------
        window.loadModel = (path) => {
            selectedModelPath = path;
            const loader = new GLTFLoader();

            console.log("🎨 Loading model:", path);

            loader.load(
                path,
                (gltf) => {
                    if (model) scene.remove(model);
                    model = gltf.scene;

                    // Store original material colors and apply initial settings
                    originalMaterialColors.clear();
                    model.traverse((child) => {
                        if (child.isMesh && child.material) {
                            child.castShadow = true;
                            child.receiveShadow = true;

                            // Store the original material color
                            originalMaterialColors.set(child, child.material.color.clone());

                            // Apply color or texture based on current settings
                            if (selectedColor.toLowerCase() === "#ffffff") {
                                if (selectedTexture && texturePath) {
                                    const textureLoader = new THREE.TextureLoader();
                                    textureLoader.load(
                                        texturePath,
                                        (texture) => {
                                            texture.encoding = THREE.sRGBEncoding;
                                            texture.wrapS = THREE.RepeatWrapping;
                                            texture.wrapT = THREE.RepeatWrapping;
                                            texture.repeat.set(1, 1);
                                            child.material.map = texture;
                                            child.material.color.copy(originalMaterialColors.get(child));
                                            child.material.needsUpdate = true;
                                        },
                                        undefined,
                                        (error) => console.error("❌ Error loading texture:", error)
                                    );
                                } else {
                                    child.material.map = null;
                                    child.material.color.copy(originalMaterialColors.get(child));
                                    child.material.needsUpdate = true;
                                }
                            } else {
                                child.material.color.set(selectedColor);
                                if (selectedTexture && texturePath) {
                                    const textureLoader = new THREE.TextureLoader();
                                    textureLoader.load(
                                        texturePath,
                                        (texture) => {
                                            texture.encoding = THREE.sRGBEncoding;
                                            texture.wrapS = THREE.RepeatWrapping;
                                            texture.wrapT = THREE.RepeatWrapping;
                                            texture.repeat.set(1, 1);
                                            child.material.map = texture;
                                            child.material.needsUpdate = true;
                                        },
                                        undefined,
                                        (error) => console.error("❌ Error loading texture:", error)
                                    );
                                } else {
                                    child.material.map = null;
                                }
                                child.material.needsUpdate = true;
                            }
                        }
                    });

                    // Calculate bounding box
                    const box = new THREE.Box3().setFromObject(model);
                    const size = box.getSize(new THREE.Vector3());
                    originalModelSize = size.clone();

                    // Apply custom scale
                    const { scale, width, height } = dimensions;
                    model.scale.set(scale, scale, scale);

                    const maxDim = Math.max(size.x, size.y, size.z);
                    if (maxDim > 0) {
                        const normalizeScale = 2 / maxDim;
                        model.scale.multiplyScalar(normalizeScale);
                    }

                    model.scale.x *= width;
                    model.scale.y *= height;
                    model.scale.z *= width;

                    box.setFromObject(model);
                    const newCenter = box.getCenter(new THREE.Vector3());

                    model.position.x = -newCenter.x;
                    model.position.y = -newCenter.y;
                    model.position.z = -newCenter.z;

                    scene.add(model);

                    const finalSize = box.getSize(new THREE.Vector3());
                    const maxFinalDim = Math.max(finalSize.x, finalSize.y, finalSize.z);
                    const cameraDistance = maxFinalDim * 2.5;
                    camera.position.set(0, cameraDistance * 0.4, cameraDistance);
                    camera.lookAt(0, 0, 0);

                    console.log(`✅ Model loaded with dimensions - W: ${width}, H: ${height}, S: ${scale}`);
                },
                (xhr) => console.log(`Loading: ${(xhr.loaded / xhr.total * 100).toFixed(1)}%`),
                (error) => {
                    console.error("❌ Error loading model:", error);
                }
            );
        };

        // ---------------------
        // 🎨 Customization Functions
        // ---------------------
        window.changeColor = (hex) => {
            selectedColor = hex;
            if (!model) return;

            model.traverse((child) => {
                if (child.isMesh && child.material) {
                    if (hex.toLowerCase() === "#ffffff") {
                        // Use texture's default color or original material color
                        if (selectedTexture && texturePath) {
                            const textureLoader = new THREE.TextureLoader();
                            textureLoader.load(
                                texturePath,
                                (texture) => {
                                    texture.encoding = THREE.sRGBEncoding;
                                    texture.wrapS = THREE.RepeatWrapping;
                                    texture.wrapT = THREE.RepeatWrapping;
                                    texture.repeat.set(1, 1);
                                    child.material.map = texture;
                                    child.material.color.copy(originalMaterialColors.get(child) || new THREE.Color(0xffffff));
                                    child.material.needsUpdate = true;
                                },
                                undefined,
                                (error) => console.error("❌ Error loading texture:", error)
                            );
                        } else {
                            child.material.map = null;
                            child.material.color.copy(originalMaterialColors.get(child) || new THREE.Color(0xffffff));
                            child.material.needsUpdate = true;
                        }
                    } else {
                        // Apply selected color, keep texture if present
                        child.material.color.set(hex);
                        if (selectedTexture && texturePath) {
                            const textureLoader = new THREE.TextureLoader();
                            textureLoader.load(
                                texturePath,
                                (texture) => {
                                    texture.encoding = THREE.sRGBEncoding;
                                    texture.wrapS = THREE.RepeatWrapping;
                                    texture.wrapT = THREE.RepeatWrapping;
                                    texture.repeat.set(1, 1);
                                    child.material.map = texture;
                                    child.material.needsUpdate = true;
                                },
                                undefined,
                                (error) => console.error("❌ Error loading texture:", error)
                            );
                        } else {
                            child.material.map = null;
                        }
                        child.material.needsUpdate = true;
                    }
                }
            });
        };

        window.applyTexture = (textureName) => {
            selectedTexture = textureName;
            texturePath = `/textures/${textureName.toLowerCase()}.jpg`;
            if (!model) return;

            const textureLoader = new THREE.TextureLoader();
            textureLoader.load(
                texturePath,
                (texture) => {
                    texture.encoding = THREE.sRGBEncoding;
                    texture.wrapS = THREE.RepeatWrapping;
                    texture.wrapT = THREE.RepeatWrapping;
                    texture.repeat.set(1, 1);
                    model.traverse((child) => {
                        if (child.isMesh && child.material) {
                            child.material.map = texture;
                            if (selectedColor.toLowerCase() === "#ffffff") {
                                child.material.color.copy(originalMaterialColors.get(child) || new THREE.Color(0xffffff));
                            } else {
                                child.material.color.set(selectedColor);
                            }
                            child.material.needsUpdate = true;
                        }
                    });
                },
                undefined,
                (error) => console.error("❌ Error loading texture:", error)
            );
        };

        // ---------------------
        // 📏 Dimension Controls
        // ---------------------
        window.changeScale = (val) => updateDimension("scale", val);
        window.changeWidth = (val) => updateDimension("width", val);
        window.changeHeight = (val) => updateDimension("height", val);

        function updateDimension(type, value) {
            dimensions[type] = parseFloat(value);
            updateModelScale();
            document.getElementById(`${type}Value`).textContent = value;
        }

        function updateModelScale() {
            if (!model || !originalModelSize) return;

            const hadModel = scene.children.includes(model);
            if (hadModel) scene.remove(model);

            model.scale.set(1, 1, 1);

            const box = new THREE.Box3().setFromObject(model);
            const size = box.getSize(new THREE.Vector3());

            const { scale, width, height } = dimensions;
            model.scale.set(scale, scale, scale);

            const maxDim = Math.max(size.x, size.y, size.z);
            if (maxDim > 0) {
                const normalizeScale = 2 / maxDim;
                model.scale.multiplyScalar(normalizeScale);
            }

            model.scale.x *= width;
            model.scale.y *= height;
            model.scale.z *= width;

            box.setFromObject(model);
            const newCenter = box.getCenter(new THREE.Vector3());

            model.position.x = -newCenter.x;
            model.position.y = -newCenter.y;
            model.position.z = -newCenter.z;

            if (hadModel) scene.add(model);

            const finalSize = box.getSize(new THREE.Vector3());
            const maxFinalDim = Math.max(finalSize.x, finalSize.y, finalSize.z);
            const cameraDistance = maxFinalDim * 2.5;
            camera.position.set(0, cameraDistance * 0.4, cameraDistance);
            camera.lookAt(0, 0, 0);

            console.log(`📏 Updated dimensions - W: ${width}, H: ${height}, S: ${scale}`);
        }

        window.resetDimensions = () => {
            Object.keys(dimensions).forEach(key => dimensions[key] = 1.0);
            ["scale", "width", "height"].forEach(id => {
                document.getElementById(`${id}Slider`).value = 1.0;
                document.getElementById(`${id}Value`).textContent = "1.0";
            });
            updateModelScale();
        };

        // ---------------------
        // 🚀 Submit Customization
        // ---------------------
        window.submitCustomization = () => {

            // Show confirmation before sending
            Swal.fire({
                title: 'Submit customization?',
                text: "Are you sure you want to submit this customization?",
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#3085d6',
                cancelButtonColor: '#d33',
                confirmButtonText: 'Yes, submit',
                cancelButtonText: 'Cancel'
            }).then((result) => {
                if (result.isConfirmed) {
                    const data = {
                        Model: selectedModelPath,
                        color: selectedColor,
                        texture: texturePath,
                        scale: dimensions.scale,
                        width: dimensions.width,
                        height: dimensions.height,
                        Id: @ViewBag.Id
                    };
                    $.ajax({
                        url: '/Home/Customization',
                        type: 'POST',
                        data: data,
                        success: function (res) {
                            if (res.success) {
                                Swal.fire({
                                    icon: 'success',
                                    title: 'Saved!',
                                    text: res.message,
                                    timer: 1500,
                                    showConfirmButton: false
                                });
                            } else {
                                Swal.fire({
                                    icon: 'warning',
                                    title: 'Oops!',
                                    text: res.message
                                });
                            }
                        },
                        error: function (xhr) {
                            Swal.fire({
                                icon: 'error',
                                title: 'Error',
                                text: 'Something went wrong. Check console for details.'
                            });
                            console.error(xhr.responseText);
                        }
                    });

                }
            });
        };


        // ---------------------
        // 🔄 Handle Resizing
        // ---------------------
        window.addEventListener("resize", () => {
            const width = canvas.clientWidth;
            const height = canvas.clientHeight;
            camera.aspect = width / height;
            camera.updateProjectionMatrix();
            renderer.setSize(width, height, false);
        });

        // ---------------------
        // 🌀 Animation Loop
        // ---------------------
        function animate() {
            requestAnimationFrame(animate);
            controls.update();
            renderer.render(scene, camera);
        }

        // Initialize
        console.log("Initializing 3D viewer...");
        animate();
        loadModel(selectedModelPath);
    </script>
}